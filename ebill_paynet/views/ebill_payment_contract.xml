<?xml version="1.0" encoding="utf-8"?>

<odoo>

  <record model="ir.ui.view" id="ebill_payment_contract_form_view">
    <field name="name">ebill.payment.contract.form (in ebill_panyet)</field>
    <field name="model">ebill.payment.contract</field>
    <field name="inherit_id" ref="ebill_base.ebill_payment_contract_form_view"/>
    <field name="arch" type="xml">
      <field name="partner_id" position="after">
        <field name="paynet_account_number">
          <!-- <attribute name="invisible" eval="[('transmit_method_id', '!=', ref('ebill_paynet.paynet_transmit_method')]"/> -->
          <!-- This one does not work -->
          <attribute name="attrs" eval="{'invisible': [('transmit_method_id', '!=', ref('ebill_paynet.paynet_transmit_method')]}"/>
        </field>
      </field>
    </field>
  </record>

</odoo>
